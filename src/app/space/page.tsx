import { FC } from "react";

interface Props {}

const SpacePage: FC<Props> = (props) => {
  const {} = props;

  return <h1 className="text-5xl font-bold text-primary">Space Page</h1>;
};

export default SpacePage;
