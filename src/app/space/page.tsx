import { FC } from "react";
import { H1 } from "@/components/UI/Typography";

interface Props {}

const SpacePage: FC<Props> = (props) => {
  const {} = props;

  return <H1>Space Page</H1>;
};

export default SpacePage;
